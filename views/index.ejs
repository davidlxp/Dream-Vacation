<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>Travel Search</title>
    <style>
        label {
            display: block;
            margin-bottom: 5px;
        }

        input,
        select {
            display: block;
            margin-bottom: 10px;
        }
    </style>
</head>

<body>
    <h1>Find Your Dream Vacation</h1>
    <form action="/search" method="post">
        <label for="origin">Origin City:</label>
        <select id="origin" name="origin" required>
            <option value="">Please select an origin city</option>
            <% var cities=['New York City', 'Los Angeles' , 'London' , 'Tokyo' , 'Sydney' ]; %>
                <% for (var i=0; i < cities.length; i++) { %>
                    <option value="<%= cities[i] %>">
                        <%= cities[i] %>
                    </option>
                    <% } %>
        </select>

        <label for="nights">Number of Nights:</label>
        <input type="number" id="nights" name="nights" min="1" max="30" required>

        <label for="budget">Budget:</label>
        <input type="number" id="budget" name="budget" min="1" max="10000" required>

        <button type="submit">Search</button>
    </form>
</body>

</html>